{
  "name": "connect-4-backend",
  "version": "0.0.0",
  "description": "* php 5.4 >= * composer [https://getcomposer.org/] * php mcrypt * nginx + php5-fpm",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "author": "Nikola Henezi",
  "license": "MIT",
  "dependencies": {
    "cookie": "^0.1.3",
    "querystring": "^0.2.0",
    "redis": "^0.12.1",
    "socket.io": "^1.3.5"
  }
}
